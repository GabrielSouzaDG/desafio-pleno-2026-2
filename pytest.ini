[pytest]
# Ancora o rootdir do pytest na raiz do repo -- sem isso, `pytest
# tests/test_X.py` (passando um caminho de arquivo, não um diretório)
# nunca sobe até aqui para achar conftest.py, e `from ingestion...`/
# `from transform...` falham com ModuleNotFoundError mesmo com o
# conftest.py existindo (descobri isso rodando de verdade dentro do
# container do Spark).
testpaths = tests
